System.gc()
apply plugin:'java'
apply plugin:'idea'
System.gc()

task copyFiles(type:Sync) {
   from 'project1/src'
   into 'build/other'
   doLast {
     println new GreatUtils().sayHello()
   }
}

gradle.buildFinished {
    println "Calling System.gc()"
    System.gc()
}

subprojects { p ->
    apply from:rootProject.file('gradle/firstlevel.gradle'), to:p
}
